databases:
  dev_url: postgres://postgres:postgres@localhost:5432/pgmt_dev
  shadow:
    auto: true

directories:
  schema_dir: schema
  migrations_dir: migrations
  baselines_dir: schema_baselines

# Object Management
objects:
  comments: true
  grants: true
  triggers: true
  extensions: true
