# Example environment configuration for pgmt development
# Copy this file to .env and modify for your local setup

# Database URL for sqlx compile-time verification
# This will be set automatically by scripts/setup-dev-db.sh to use an available port
# You can also set it manually if you have a specific database to use
DATABASE_URL=postgres://postgres:pgmt_dev_password@localhost:5433/pgmt_dev

# Docker configuration
DOCKER_HOST=unix:///var/run/docker.sock

# PostgreSQL version for testing and development
PG_VERSION=15-alpine

# Optional: Rust log level for development
RUST_LOG=debug

# Development database configuration (used by setup-dev-db.sh)
# DB_PORT=5433           # Uncomment to force a specific port, otherwise auto-detected
DB_PASSWORD=pgmt_dev_password
DB_NAME=pgmt_dev
POSTGRES_VERSION=15-alpine